{
  "ignorePatterns": [{ "pattern": "^https?://" }, { "pattern": "^mailto:" }],
  "comment": "External and example URLs (the project and library repos, localhost demo links, third-party docs) are not network-checked; this config validates only the relative file links between docs and into the source tree."
}
